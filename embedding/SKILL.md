---
name: embedding
description: Implement text, image, and video embedding capabilities using the coze-coding-dev-sdk package. Use this skill when the user needs to convert text, images, or videos into vector representations for semantic search, similarity comparison, clustering, recommendation systems, or RAG (Retrieval-Augmented Generation) applications. Supports multimodal (text + image + video) embeddings, multi-vector output, sparse embeddings, and customizable dimensions with instructions.
license: MIT
---

# Embedding Skill

This skill guides the implementation of embedding functionality using the coze-coding-dev-sdk package, enabling powerful vector representation capabilities for text, images, and videos.

## Overview

The Embedding skill allows you to convert text, images, and videos into high-dimensional vector representations that capture semantic meaning. These embeddings can be used for:

## Supported Languages

This skill supports both Python and TypeScript SDKs. Choose the appropriate version for your project. **You MUST refer to the detailed guide in the corresponding SDK folder.**

- **Python SDK**: See [python/README.md](python/README.md) for installation, client initialization, and usage examples.
- **TypeScript SDK**: See [typescript/README.md](typescript/README.md) for installation, client initialization, and usage examples.